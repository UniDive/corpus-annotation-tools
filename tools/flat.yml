Tool ID:
  Tool name: 'FLAT: FoLiA Linguistic Annotation Tool'
  Logo: http://proycon.github.io/folia/style/icon.png
  Short description: Web-based linguistic annotation environment based around the FoLiA format (http://proycon.github.io/folia), a rich XML-based format for linguistic annotation.
  Website: https://github.com/proycon/flat
  Other useful links:
    - Folia format: http://proycon.github.io/folia/
  Developer: 'Yes'
  Example projects:
    - PARSEME: https://aclanthology.org/2024.lrec-main.184/
  Code repository: https://github.com/proycon/flat

Software:
  License: GPL-3.0
  Active maintenance: Legacy
  Software type: Web-based application
  Operating system:
    - Windows
    - macOS
    - Linux
  Software availability:
    - The software should be run by the user (local machine or own server)
  Offline usage: 'No'

Data support:
  Input format:
    - Raw text
    - CONLL-U
    - XML
    - TSV
    - FoLiA XML
  Integration-of-other-resources:
  Data export:
    - The data can be exported.
    - The data can be stored with a third party storage platform (e.g. Git repository)

Diversity features:
  Diversity features:
    - Language-independence
    - Writing script support
    - LTR
    - RTL
    - unicode support
  Language of the interface:
    - English

Annotation:
  Segmentation:
    - word-level
  Annotation types:
    - Tokens (e.g. part-of-speech tag morphological features)
    - Spans (e.g. names entities),
    - Multi-tokens (e.g. multi-word expressions
    - Relations (e.g. dependencies)
    - Chains (e.g. coreferences)
  Annotation scope:
    - Annotation inside sentences
  Tagset-customization: 'Yes'

User Experience:
  Annotation mode:
    - Mouse (e.g. drag & drop)
  Keyboard shortcuts: 'No'
  Annotation display:
  Automatic pre-annotation: 'No'
  Automatic recommendation: 'No'
  Collaborative annotation:
    - annotators working independently on their own annotations of the same data.
  Project management: partial
  User roles: 'Yes'
  Comparison of annotations: 'No'
  Agreement calculation: 'No'
  Querying: 'Yes'
  Mass editing: 'Yes'

UD/PARSEME specifics:
  UD specific annotations:
    - Lemmas
    - UPOS
    - FEATS
    - XPOS
    - Multi-word tokens
    - Basic dependencies
  UD validation: 'No'
  Other annotation layers: ''
  PARSEME specific annotations:
    - Discontinuous expressions
    - Nested expressions
    - Overlapping expressions
  Consistency check: 'No'

