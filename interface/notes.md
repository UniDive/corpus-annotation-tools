Features (right panel):
- [X] light grey text for feature values (e.g. Free span annotation) is hard to read (suggestion: change to the blue font you have for the same values in the tool description) -- DONE, changed to dark grey. Blue was not viable because of the hovering
- [X] LTR and RTL could be spelled-out – we have enough space -- DONE
- [X] In Software, could we change the ranking from more to less important: Active > License > Software type > Operating system > Offline usage > Software availability (we should probably be renamed to Software Access for clarity)
- [X] We should move the UD / PARSEME tiles to the end of the list, because very specific
- [X] Annotation of Universal Dependencies à can just be Universal Dependencies --- DONE
- [X] Within UD, UD-specific annotatios before UD validation
- [X] Withing PARSEME, same as above
- [X] Within Annotation Analysis, change in priority: Comparison first

Tool details:
- [X] Currently very confusing to have the metadata (e.g. descriptions and/or useful links) both in the left and right column of the ‘tool details’ panel (bottom-left) à I propose to move all metadata to the left side.
This new left side:
- [X] When doing so, keep just one of the two textual descriptions we currently have (i.e. the one currently on the right, which is how the respondents described their tool).
- [X] When moving it to the left side, we could visualize the code repository link the same as all other links, to avoid too much visual clutter.
- [X] Most users are interested in the main website of the tool, which is currently ‘hidden’ under the tool name, so I would suggest to also spell-it out explicitely in the tool description or something similar to increase visibility of this main link.
The remaining right side:
- [X] Instead of the metadata, which now moves to the left, we introduce a simple heading ‘Tool features’, so that the users immeadiately know what the list below is about
- [X] It would be useful to introduce area subheadings to help navigate through the long list of features

------------------------

Other things (only for perfectionists, lowest priority, but could be done before LREC if accepted):
- [x] Design: update with credits, links, logos
Features:
- [X] Add the same type of explanatory descriptions of features as in the questionnaire? E.g. User roles à Does the tool enable the users to be differentiated by their roles (e.g. admin, manager, annotator, curator)?
- [ ] Reevaluate the 1/1 features – are those really unique features or are just instances of other options (which could be renamed if needed)
Tool details:
- [X] (ONLY IF EASILY DONE) Some features have long lists of possible values, which fall into a new line if too long. Any way the newline could be indented, so that it is clear it falls within a feature?